# MISS.md — 同じ失敗を二度やらないための台帳

実際に**ユーザーの手元で起きた**不具合のうち、「原因が構造的で、放っておけば別の形でまた出る」ものだけを記録する。修正の履歴は git が持っているので、ここに書くのは **次に同じ穴へ落ちないための規則**と、**その穴をどう見つけたか**。

新しい事例は末尾に追記する。日付は発覚日。

---

## 恒久ルール（事例から一般化したもの）

R1. **教材の「中身が変わったか」の比較に、書き込み時刻を混ぜない。**
`updatedAt` / `pageLayout.overlay.updatedAt` は内容ではない。保存経路も打鍵も `{...doc, updatedAt: now}` という別コピーを作り、そのコピーだけを保存済みの姿として覚えるので、画面の文書とディスク基準の時刻は**常にズレている**。比較は `apps/desktop/src/lib/document-equivalence.ts` の `areSigmaDocumentsEquivalent` / `comparableDocumentValue` を通す。新しい比較・ハッシュ・鮮度判定を足すときも同じ射影を使う。

R2. **AI編集の承認は、いま開いている 1 ファイルの中だけで解決する。**
競合を理由に教材ファイルを増やさない。競合した単位は承認された内容を採り、採用前の文書を undo エントリへ積んで Ctrl+Z で戻せるようにする。新しい教材ファイルを作ってよいのは、書き戻す先が消えた場合 (`switchAwayFromDeletedFile`) だけ。

R3. **「安全側に倒すフォールバック」は、必ず数えられるようにする。**
退避・全文リロード・提案の再生成のような重い救済経路は、成功扱いで静かに走ると常時発火していても誰も気づかない。フォールバックを書くときは (a) 発火が e2e から観測できること、(b) 正常系のテストで発火回数 0 を期待すること、をセットにする。

R4. **重要な不変条件は、再現fixtureと保存結果で検証する。**
想定だけで頻度や影響を判断せず、失敗経路を再現して確認する。

R5. **モックは、実装が実際にやることを再現する。**
e2e のモックが本物より「おとなしい」と、退行をすり抜ける。#001 では main プロセスが承認保存のたびに `updatedAt` を進めるのに、モックが進めていなかったため、退避教材が量産される状態でも e2e は緑だった。

R6. **独立図形へ正規化した所有データは、派生計算の直前に必ずhydrateする。**
グラフラベルの本文はgraph specではなく、グラフが所有するtext shapeが正本。配置・寸法・表示状態を再計算するhelperへ、本文を消した永続化用specをそのまま渡してはいけない。`hydrateGraphSpecWithOwnedLabelTexts`で一時specを作り、永続化用のgraph shape自体は変更しない。WebMCP挿入→人間の設定変更までを通すe2eで、軸・点・注記・式ラベルが全種類残ることを検査する。

R7. **同じ文書構造を流すレイアウトエンジンは、keep-together の粒度と判定述語を共有する。**
問題全体・問題エリア・内部ブロックのどれを atomic にするかが経路ごとに違うと、ある表示だけで改ページ不能になる。問題エリアは `isProblemAreaFlowEligible` を共通の意味として使い、エリア固有の枠や予約高だけをそのエリアへ適用する。atomic / flow の切替には、DOM から適用済み gap を引いた高さとページ定数以外を入れない。

R8. **断片の描画 chrome は、断片ごとに改ページ容量から予約する。**
開いた枠の底 padding や border は本文ブロックの実測高に含まれない。全体の先頭と末尾で一度だけ予約すると、中間片が用紙下端を越えたり、最終片と後続ユニットが丸め誤差で重なる。断片の使用可能高から chrome を先に引き、後続の開始は描画下端以降へ切り上げる。

R9. **編集可能な段組の列所属を、編集後の平坦な子配列やDOMの折り返しから逆算しない。**
列はSigmaDocの`layout.columnStartIds`、幅は`layout.columnWidths`を正本にする。構造編集は編集前の列所属を投影へ運んだまま対象列を再構築し、消えた先頭IDを均等再分配で補わない。各列は別の編集面とページカーソルを持ち、`pagination.break`を列境界の代用にしない。グリップ・下余白線は外側レーンを先に決め、そのレーン内の同じ高さにある最も内側の1単位だけへ出す。

R10. **ブロック単位配置の編集面は、新規ブロックの配置が確定するまでキャレット追従を保留する。**
対象判定は「外側のページが段組みか」ではなく、その編集面自身が配置装飾を使うかで行う。ページ段組みと部分段組みの独立カラムを同じ経路で扱い、未配置のDOM座標でスクロール量を決めない。

R11. **常設の編集面は、自分以外が書いた文書変更を自分で拾う。**
`OverlayCanvasEditor` のような「自前の作業状態を持ち、まとめて文書へ書き戻す」面は、`historyRevision` (教材切替・undo・外部マージ) だけを再読込の合図にしてはいけない。AI提案の適用・貼り付け・別面からの `commitDocumentChange` は `historyRevision` を進めないので、その面が常に表示されているモード (ホワイトボード) では**文書には入ったのに画面に出ない**。文書の overlay が「自分の書き込みの反響 (配列同一性)」でも「同じ内容」でもなく変わったら外部変更として採用する。検証は「文書 (inspect_document) にはある / DOM には無い」の差で見つける。

R12. **subpath配信では、HTMLが実際に要求する全asset rootを同じbuildから更新する。**
`/sigma/` に静的出力を置いても、生成HTMLが `/_next/...` を参照する構成では `public/sigma/_next` だけの更新では足りない。`public/_next` など配信ラッパーが持つmirrorも同じbuildから同期し、archive内の両配置、実URLのhashed chunk、起動画面の消失まで確認する。routeのHTTP 200だけでデプロイ成功と判定しない。

R13. **編集面と印刷面は、1ブロックの本文を無制限に組版しない。**
改行も空白も無い数メガバイトの文字列（JSON書き出しのbase64など）を1段落として載せると、ChromiumのFontationsが巨大なglyphバッファを確保してRendererがOOMで落ちる。SigmaDocの本文は残し、表示だけを上限付きプレースホルダへ置き換える。上限を超えた本文の往復は previous ブロックから復元し、プレースホルダを正本に書いてはいけない。

---
